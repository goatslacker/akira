module.exports = (function () {
    var Identifier;
    Identifier = function (name, core, rest) {
        if (core == null)
            core = false;
        if (rest == null)
            rest = false;
        return {
            type: 'Identifier',
            name: name,
            __core: core,
            __rest: rest
        };
    };
    return Identifier;
}());