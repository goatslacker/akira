module.exports = function (fn, arr) {
  var index = -1;
  var length = arr.length;
  var results = [];

  while (++index < length) {
    results = results.concat(fn(arr[index], index, arr));
  }

  return results;
}
