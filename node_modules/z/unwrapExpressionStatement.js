function unwrapExpressionStatement(expr) {
  if (Array.isArray(expr)) {
    return unwrapExpressionStatement(expr[0])
  } else {
    return expr.type === 'ExpressionStatement' ? expr.expression : expr
  }
}

module.exports = unwrapExpressionStatement
