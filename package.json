{
  "name": "rini",
  "author": "Josh Perez <josh@goatslacker.com>",
  "version": "0.0.1",
  "description": "project rini",

  "scripts": {
    "pretest": "bin/memory compile src/grammar.mem > lib/grammar.js && node lib/grammar.js",
    "test": "bin/memory test"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/goatslacker/rini.git"
  },

  "engines": {
    "node": ">= 0.4.0"
  }
}
