{
  "name": "memory",
  "author": "Josh Perez <josh@goatslacker.com>",
  "version": "0.0.1",
  "description": "tiny language that compiles to javascript",

  "scripts": {
    "start": "bin/memory compile src/",
    "postinstall": "node lib/grammar.js",
    "stop": "git co lib/grammar.js",
    "test": "bin/memory test"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/goatslacker/rini.git"
  },

  "dependencies": {
    "esprima": "*",
    "escodegen": "*"
  },

  "devDependencies": {
    "jison": "*",
    "nodewatch": "*"
  },

  "bin": { "memory": "./bin/memory" },

  "engines": {
    "node": ">= 0.4.0"
  }
}
